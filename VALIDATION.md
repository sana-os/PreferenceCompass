# Validation — Arabic edition, public release 1.2.0

Commands: node scripts/build.mjs; node tests/check.mjs.

Passed: 9 editions × 100 aligned questions, four stable options each, complete JSON/Markdown exports, response status handling, original text/language preservation, variable-length later updates, application handlers under a minimal DOM, 1,178 local HTML links and 54 sitemap pages. UI placeholders match English; all editions link to Arabic. Arabic pages have RTL attributes; Latin formulas are isolated. Mixed Arabic/English free text survives an interface-language change unchanged, with ar provenance. Legacy schemas 1.1.0 and 1.1.1 are preserved byte-for-byte; new schema 1.1.2 permits ar. Protocol 1.1.0 is unchanged.

The tests check the JSON Schema vocabulary used by this schema. They do not verify the truth of evidence in future AI-written updates. Independent native-speaker review and real-browser visual QA have not been performed. Browser binaries are not installed in this environment; direction attributes and responsive CSS were inspected as source. No production deployment is claimed.
